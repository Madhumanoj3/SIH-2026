# SmartSense SIH-26 — Project Cleanup Audit (Phase 1 + 2)

**Status: INSPECTION ONLY. No files were moved, deleted, or modified while producing this report.**

Generated by inspecting the actual files on disk (imports, hardcoded paths, model
`n_features_in_`, real CSV columns, byte-identical duplicate checks) — not assumed
from filenames. Every "NEEDS REVIEW" item explains exactly why it couldn't be
resolved safely without your input.

---

## 1. Current structure (verified fresh, this session)

```
SIH-26/
├── Drive Mode/
│   ├── .venv/            (Python 3.11.9, 598MB)
│   ├── dataset/          (DROZY/DD-Database, 745MB — see duplication finding)
│   ├── models/           (drozy_xgboost_vigilance.joblib, drozy_scaler.joblib, drozy_model_metadata.json)
│   ├── processed/        (window CSVs, feature CSVs, dd_continuous_vigilance/ subfolder)
│   ├── results/          (dd_continuous_vigilance/, vigilance/, deployment_predictions.csv)
│   └── scripts/          (39 .py files + __pycache__)
│
└── Rest Mode/
    ├── dataset/          (DREAMT dataset, 101GB, 100 subject CSVs)
    └── scripts/
        ├── models/       (9 .joblib + 2 feature-importance CSVs)
        ├── processed/    (train/test splits + many result CSVs)
        └── *.py          (19 files + __pycache__)
```

**Important discrepancy from your message, reported per "do not guess":** your message
described SEED-VIG files as possibly still present and needing archival classification.
I searched all of `Drive Mode/` and `Rest Mode/` and **found zero SEED-VIG artifacts**
(no `.mat` files, no 17-channel/25-bin structures, no `New Drive Mode/` folder — it no
longer exists at the SIH-26 top level). Whatever SEED-VIG work existed previously
appears to have already been removed or relocated outside this project tree before
this session started. I have not searched outside `D:\SIH-26` for it — tell me if you
want that.

---

## 2. Final Model Packaging Preparation — verified artifact inventory

### DRIVE MODE

| Item | Value (verified from the actual files) |
|---|---|
| 1. Model filename | `models/drozy_xgboost_vigilance.joblib` |
| 2. Model type | `xgboost.sklearn.XGBRegressor` (confirmed by loading it) |
| 3. Scaler filename | `models/drozy_scaler.joblib` (`sklearn.preprocessing.StandardScaler`) |
| 4. Feature count | **28** (model, scaler, and metadata all agree: `n_features_in_ == 28`) |
| 5. Feature names | eeg_mean, eeg_std, eeg_var, eeg_rms, eeg_range, eeg_total_power, eeg_delta_power, eeg_rel_delta, eeg_theta_power, eeg_rel_theta, eeg_alpha_power, eeg_rel_alpha, eeg_beta_power, eeg_rel_beta, eeg_theta_alpha, eeg_theta_beta, eeg_spectral_entropy, eog_mean, eog_std, eog_rms, eog_var, eog_amplitude, eog_zcr, eog_lowfreq_power, eog_spectral_entropy, eog_blink_count, eog_blink_rate_per_min, eog_blink_duration_mean |
| 6. Metadata availability | `models/drozy_model_metadata.json` — present, complete, already self-documents the "not clinically validated" limitation and cites the LOSO mean-subject R²=-0.563 |
| 7. Inference script | `scripts/predict_vigilance.py` (batch) + `scripts/realtime_simulator.py` (streaming) |
| 8. Model loads? | **YES** — loaded cleanly, no warnings |
| 9. Scaler loads? | **YES** — loaded cleanly, `n_features_in_=28`, matches model |
| 10. Missing artifacts | None |
| 11. Duplicate/obsolete artifacts | None at the models/ level |
| 12. Potential incompatibility | None found |

Target formula also verified from the metadata file: `vigilance(t) = 100*(1-exp(-d(t)/60))`
where `d(t)` = time-distance to nearest self-report event — this is `target_B_60`, an
event-derived proxy, **not PERCLOS**. Consistent with what you told me.

### REST MODE

| Item | Value (verified from the actual files — **do not assume**, per your instruction) |
|---|---|
| 1. Model filename | `scripts/models/xgboost_n2.joblib` (identified as final by tracing what `replay_test.py`/`replay_multiple.py` actually load — see §4) |
| 2. Model type | `xgboost.sklearn.XGBClassifier`, **binary N2 vs non-N2 classifier** — not a regressor. `train_xgboost.py` reports accuracy/precision/recall/F1/ROC-AUC, not MAE/R². This is a materially different model *kind* than Drive Mode's continuous regressor. |
| 3. Scaler filename | **None exists.** `train_xgboost.py` feeds raw features directly into `XGBClassifier` — no `StandardScaler` is fit anywhere in the training code. This may be intentional (tree models don't require feature scaling) rather than a missing artifact — **[NEEDS REVIEW]: confirm whether a scaler was ever intended for Rest Mode, since your target structure lists `rest_mode_scaler.joblib`.** |
| 4. Feature count | **32** (verified directly from `scripts/processed/train.csv`: 38 total columns − 6 metadata columns `subject_id, epoch, start_time, end_time, sleep_stage, label` = 32; `train_xgboost.py` also hard-asserts `!= 32 → raise ValueError`) |
| 5. Feature names | eeg_mean, eeg_std, eeg_variance, eeg_rms, eeg_min, eeg_max, eeg_range, eeg_energy, eeg_delta_power, eeg_theta_power, eeg_alpha_power, eeg_beta_power, eeg_delta_relative, eeg_theta_relative, eeg_alpha_relative, eeg_beta_relative, eeg_theta_alpha_ratio, eeg_theta_beta_ratio, eeg_delta_theta_ratio, eeg_spectral_entropy, eog_mean, eog_std, eog_variance, eog_rms, eog_min, eog_max, eog_range, eog_energy, eog_low_frequency_power, eog_low_frequency_relative, eog_spectral_entropy, eog_zero_crossings |
| 6. Metadata availability | **None.** No `rest_mode_metadata.json` or equivalent exists anywhere. Feature list above only exists implicitly as CSV column order. |
| 7. Inference script | Chain: `replay_test.py`/`replay_multiple.py` (loads model) → `sleep_state_engine.py` (temporal smoothing) → `smart_alarm.py` (wake-during-N2 alarm logic) |
| 8. Model loads? | **YES, but with a warning**: `UserWarning` from XGBoost that this pickle was saved by an older XGBoost version than the one installed (3.2.0) and recommends re-exporting via `Booster.save_model` — see §5 risk. |
| 9. Scaler loads? | N/A — doesn't exist |
| 10. Missing artifacts | Scaler (possibly N/A — see NEEDS REVIEW above), metadata JSON, requirements.txt |
| 11. Duplicate/obsolete artifacts | `xgboost_fold_1..5.joblib` (per-fold CV artifacts, not final), `random_forest_n2/eeg_eog/eeg_only/eog_only.joblib` (ablation-study comparison models, not final) — see §4 |
| 12. Potential incompatibility | XGBoost pickle version mismatch warning (§5) |

**Combined `smartsense_models.pkl` packaging is NOT ready yet** given: Rest Mode has no metadata file, no confirmed scaler decision, and a version-compatibility warning on its model pickle. Per your instruction, I have not created it.

---

## 3. Classification: Drive Mode

| Path | Classification | Reason |
|---|---|---|
| `models/drozy_xgboost_vigilance.joblib` | **KEEP** | Final deployment model, loads clean, referenced by predict_vigilance.py & realtime_simulator.py |
| `models/drozy_scaler.joblib` | **KEEP** | Final deployment scaler, loads clean, 28 features match model |
| `models/drozy_model_metadata.json` | **KEEP** | Feature order + provenance + honesty warning, read at inference time |
| `scripts/predict_vigilance.py` | **KEEP** | Final batch inference entry point |
| `scripts/realtime_simulator.py` | **KEEP** | Final streaming inference entry point |
| `scripts/vigilance_trend.py` | **KEEP** | Imported by realtime_simulator.py; hardcodes alarm thresholds sourced from results/dd_continuous_vigilance/alarm_grid_search.csv |
| `scripts/train_deployment_model.py` | **KEEP** | Reproduces the exact deployment model from processed/dd_continuous_vigilance/features_with_targets_w8.csv |
| `scripts/dd_vigilance_features.py` | **KEEP** | **Hidden hard dependency** — imported by predict_vigilance.py, realtime_simulator.py, AND test_deployment_pipeline.py. Not just a research script. |
| `scripts/dd_vigilance_targets.py` | **KEEP** | **Hidden hard dependency** — imported by test_deployment_pipeline.py |
| `scripts/test_deployment_pipeline.py` | **KEEP** | End-to-end pipeline test harness; imports the three files above |
| `dataset/*.edf`, `*_annotations.edf` (root level, 40 files) | **KEEP** | Source dataset, referenced directly by `DATASET_DIR = Path("dataset")` in extract_features.py, build_all_recordings.py, dd_vigilance_features.py |
| `dataset/README.md` | **KEEP** | Official DROZY/DD-Database documentation (verified: matches published DROZY abstract) |
| `processed/dd_continuous_vigilance/features_with_targets_w8.csv` | **KEEP** | Direct input to train_deployment_model.py — needed to reproduce the exact deployed model |
| `results/dd_continuous_vigilance/*` (all 14 files) | **ARCHIVE** | Full LOSO research trail (FINAL_REPORT.md, DEPLOYMENT_MODEL.md, metrics, predictions) — valuable documentation/provenance, not needed at runtime except alarm_grid_search.csv's thresholds (already hardcoded into vigilance_trend.py) |
| `results/deployment_predictions.csv` | **ARCHIVE** | Output of a predict_vigilance.py run — regenerable, not an input to anything |
| `dataset/doi_10_5061_dryad_5tb2rbp9c__v20230825 (1)/` (249MB, 40 files) | **SAFE TO DELETE** | Byte-identical duplicate of dataset root (verified: `01M_1.edf` identical size/timestamp in both locations). No script references this nested path — all scripts use `Path("dataset")` directly. Regenerable from the zip below if ever needed. |
| `dataset/doi_10_5061_dryad_5tb2rbp9c__v20230825 (1).zip` (249MB) | **ARCHIVE** (not delete) | The original download archive. Redundant with the dataset already being present and working, but I'm not calling a 249MB *original source archive* safe-to-delete without your explicit sign-off — recommend archiving it (e.g. `archive/drive_mode_experiments/raw_zip/`) rather than deleting, in case the extracted copies ever get corrupted. |
| `processed/one_channel_*`, `processed/features.csv`, `processed/features_prepared.csv`, `processed/baseline_predictions.csv`, `processed/vigilance_features_c4ref_locroc.csv`, `processed/vigilance_event_timing_analysis.json`, `processed/dd_continuous_vigilance/features_w16.csv`, `features_w30.csv`, `features_with_targets_w16.csv`, `features_with_targets_w30.csv`, `target_definitions_w*.json`, `dataset_audit.json` | **ARCHIVE** | Window-size comparison (8/16/30s) and earlier feature-engineering iterations that preceded the final w8 choice — valuable for "why w8" documentation, not used by the final pipeline |
| `processed/01M_1_windows.csv` ... `10M_2_windows.csv` (20 files), `processed/all_windows.csv` | **ARCHIVE** | Intermediate windowing outputs, regenerable from dataset/ via build_all_recordings.py; not read by predict_vigilance.py/realtime_simulator.py (those extract features live from raw EDF) |
| `results/vigilance/*` (6 files) | **ARCHIVE — likely superseded** | Timestamped ~40 min *before* `results/dd_continuous_vigilance/` on the same day; overlapping filenames (`alarm_grid_search.csv`, `trend_features.csv`, `trend_quality_metrics.json`) contain **different numbers**, not identical copies — an earlier iteration of the same analysis. `vigilance_trend.py` cites `dd_continuous_vigilance/alarm_grid_search.csv` specifically as its source, not this folder. Recommend archiving as "vigilance_v1_superseded" rather than deleting, since content differs from the final version. |
| `scripts/analyze_all_annotations.py`, `analyze_baseline.py`, `analyze_event_timing_for_vigilance.py`, `analyze_events.py`, `cluster_events.py`, `count_episodes.py`, `plot_event.py`, `inspect_annotations.py`, `inspect_edf.py`, `compare_features.py`, `train_baseline.py`, `train_vigilance_loso.py`, `trend_alarm_eval.py`, `trend_quality_eval.py`, `dd_vigilance_audit.py`, `dd_vigilance_screen_targets.py`, `dd_vigilance_full_models.py`, `dd_vigilance_window_compare.py`, `dd_vigilance_within_subject.py`, `dd_vigilance_personalization.py`, `dd_vigilance_trend_alarm.py`, `build_one_recording.py`, `build_all_recordings.py`, `build_vigilance_features.py`, `extract_features.py`, `prepare_features.py` | **ARCHIVE** | Confirmed via import-graph search: none of these are imported by any of the 6 KEEP scripts. Pure research/EDA/model-selection history — valuable for explaining *why* w8/C4-Ref/28-features were chosen, but not runtime dependencies. |
| `scripts/__pycache__/*` | **SAFE TO DELETE** | Compiled bytecode cache, auto-regenerated on next run |
| `.venv/` | **KEEP for now** | Do not delete per your instruction — see §6 |

---

## 4. Classification: Rest Mode

| Path | Classification | Reason |
|---|---|---|
| `scripts/models/xgboost_n2.joblib` | **KEEP** | Final model — traced via replay_test.py/replay_multiple.py, the only scripts that actually load a model file |
| `scripts/models/xgboost_feature_importance.csv` | **ARCHIVE** | Diagnostic output of training this model, not needed at inference |
| `scripts/replay_test.py`, `replay_multiple.py` | **KEEP** | Load xgboost_n2.joblib, produce replay_results.csv |
| `scripts/sleep_state_engine.py` | **KEEP** | Reads replay_results.csv → temporal smoothing → sleep_state_results.csv |
| `scripts/smart_alarm.py` | **KEEP** | Reads sleep_state_results.csv → final wake-during-N2 alarm decision |
| `scripts/feature_extractor.py` | **NEEDS REVIEW** | Almost certainly the script that produces the 32-feature CSVs, but I have not traced every one of its call sites yet — before deleting/archiving anything that touches it, confirm it's not imported elsewhere |
| `scripts/models/random_forest_n2.joblib` | **ARCHIVE** | Alternate algorithm for the same N2 task; `random_forest_n2.joblib` never loaded by replay_test/replay_multiple |
| `scripts/models/random_forest_eeg_eog.joblib`, `random_forest_eeg_only.joblib`, `random_forest_eog_only.joblib` | **ARCHIVE** | Ablation-study models (produced by train_ablation.py) comparing EEG-only/EOG-only/combined — valuable for the "why both signals" justification, not final artifacts |
| `scripts/models/random_forest_feature_importance.csv` | **ARCHIVE** | Diagnostic output |
| `scripts/models/xgboost_fold_1.joblib` ... `xgboost_fold_5.joblib` | **ARCHIVE** | Per-fold cross-validation artifacts from cross_validate_xgboost.py — valuable for reported CV metrics provenance, not the final single model |
| `scripts/processed/train.csv`, `test.csv` | **KEEP** | Direct inputs to train_xgboost.py; needed to reproduce the exact final model / verify the 32-feature claim |
| `scripts/processed/dreamt_features_all.csv` | **ARCHIVE** (tentative) | Looks like the pre-split full feature table `train.csv`/`test.csv` were made from — **[NEEDS REVIEW]: confirm make_split.py's input is this file before archiving**, since if the split isn't reproducible without it, it should be KEEP instead |
| `scripts/processed/ablation_results.csv`, `alarm_logic_comparison.csv`, `feature_verification_S002_epoch120.csv`, `realistic_smart_alarm_details.csv`, `realistic_smart_alarm_summary.csv`, `replay_results.csv`, `sleep_state_results.csv`, `smart_alarm_results.csv`, `smart_wake_window_details.csv`, `smart_wake_window_summary.csv`, `xgboost_5fold_results.csv`, `xgboost_5fold_summary.csv`, `xgboost_oof_predictions.csv`, `xgboost_results.csv` | **ARCHIVE** | Evaluation/research outputs — `replay_results.csv` and `sleep_state_results.csv` are regenerated fresh each time the KEEP chain runs, so the versions currently on disk are just the last run's output, not permanent inputs |
| `scripts/build_all_features.py`, `build_features.py`, `check_alarm_sequence.py`, `check_dataset.py`, `compare_alarm_logic.py`, `cross_validate_xgboost.py`, `evaluate_realistic_smart_alarm.py`, `evaluate_smart_wake_window.py`, `make_split.py`, `train_ablation.py`, `train_random_forest.py`, `verify_features.py` | **ARCHIVE** | Research/training/evaluation scripts not in the final replay→sleep_state→alarm chain |
| `scripts/train_xgboost.py` | **ARCHIVE, but keep accessible** | Produces the final model, so it's important for reproducibility, but it currently has a broken hardcoded path (see §5) so it cannot be re-run as-is today |
| `scripts/__pycache__/*` | **SAFE TO DELETE** | Compiled bytecode cache |
| `dataset/dreamt-dataset-.../data_100Hz/*.csv` (100 files, ~101GB total) | **NEEDS REVIEW** | This is the raw DREAMT source dataset — logically "ARCHIVE-tier" (not needed at runtime, only for retraining), but 101GB is large enough that I am flagging the *decision to move it at all* for your explicit call rather than silently proposing to relocate that much data. Per your rule #11, dataset contents are not to be modified regardless. |

---

## 5. Risks / issues found (concrete, verified — not speculative)

1. **Systemic hardcoded absolute paths in Rest Mode.** 17 of ~20 scripts (including `train_xgboost.py`, `smart_alarm.py`, `sleep_state_engine.py`) hardcode `BASE_DIR = r"D:\Rest Mode"` — missing the `SIH-26` folder. The actual project lives at `D:\SIH-26\Rest Mode`. This means **none of these scripts can currently be re-run as-is** — they'd fail to find their input files. The already-trained model/CSVs on disk are unaffected and still valid; this only blocks *reproduction*. I have not fixed this (Phase 1 is inspection-only) — flagging for your decision.
2. **XGBoost pickle version-compatibility warning on `xgboost_n2.joblib`.** It loads successfully today, but XGBoost itself warns the pickle was written by an older version than the currently-installed 3.2.0 and recommends re-exporting via `Booster.save_model` before this breaks in a future XGBoost release. `drozy_xgboost_vigilance.joblib` (Drive Mode) shows no such warning.
3. **No `requirements.txt` anywhere** in Drive Mode or Rest Mode. I captured Drive Mode's actual installed versions via `pip freeze` (see below) so one can be generated later — not created yet, per instructions.
4. **Rest Mode has no dedicated `.venv`.** Scripts currently run against whatever Python is on PATH; package versions used to train the existing models are not pinned anywhere.
5. **Rest Mode has no scaler and no metadata JSON**, unlike Drive Mode. May be correct (tree model, no scaling needed) rather than missing — needs your confirmation, not an assumption.
6. **Drive Mode's DROZY dataset is present 3 times**: original zip (249MB) + extracted "doi_10_5061..." folder (249MB) + loose copy at dataset root (249MB) actually used by all scripts = 745MB total for one 249MB dataset.
7. **`results/vigilance/` appears to be an earlier, superseded run** of the same analysis now in `results/dd_continuous_vigilance/`, with different numbers in overlapping-named files — not a plain duplicate, an earlier iteration.
8. **No top-level `SIH-26/README.md`** describing the two-mode project structure.

**Drive Mode's actual installed package versions** (captured via `pip freeze`, for a future requirements.txt):
`xgboost==3.2.0, scikit-learn==1.9.0, joblib==1.6.0, pandas==3.0.5, numpy==2.4.6, scipy==1.17.1, mne==1.12.1, pyEDFlib==0.1.42, matplotlib==3.11.1` (full list captured, abbreviated here).

---

## 6. Virtual environment

- `Drive Mode/.venv`: Python 3.11.9, 598MB, contains mne/pyEDFlib/xgboost/sklearn/pandas/matplotlib — actively used and needed for Drive Mode scripts to run. **Not deleted**, per instructions.
- `Rest Mode`: **no `.venv` found.** Scripts rely on an unpinned system/global Python. Recreating a clean, pinned environment is explicitly **not part of this cleanup** per your instructions — flagged only.

---

## 7. Proposed final structure (target only — nothing moved yet)

```
SIH-26/
├── Rest Mode/
│   ├── dataset/                          (unchanged — 101GB, not touched)
│   ├── models/
│   │   ├── xgboost_n2.joblib             (renamed? — NEEDS YOUR DECISION, see below)
│   │   └── xgboost_feature_importance.csv
│   ├── scripts/
│   │   ├── feature_extractor.py
│   │   ├── replay_test.py / replay_multiple.py
│   │   ├── sleep_state_engine.py
│   │   └── smart_alarm.py
│   ├── requirements.txt                  (to be generated later)
│   └── README.md                         (to be written later)
│
├── Drive Mode/
│   ├── .venv/                            (unchanged)
│   ├── dataset/                          (de-duplicated — see §3)
│   ├── models/                           (unchanged, already clean)
│   ├── scripts/
│   │   ├── predict_vigilance.py
│   │   ├── realtime_simulator.py
│   │   ├── vigilance_trend.py
│   │   ├── train_deployment_model.py
│   │   ├── dd_vigilance_features.py      (dependency, kept alongside)
│   │   ├── dd_vigilance_targets.py       (dependency, kept alongside)
│   │   └── test_deployment_pipeline.py
│   ├── results/
│   │   └── dd_continuous_vigilance/      (kept as final documentation, or moved to archive/ — YOUR CALL)
│   ├── requirements.txt                  (to be generated later)
│   └── README.md                         (to be written later)
│
├── archive/
│   ├── drive_mode_experiments/           (all ARCHIVE-tagged Drive Mode scripts/processed/results)
│   ├── rest_mode_experiments/            (all ARCHIVE-tagged Rest Mode scripts/models/processed)
│   ├── seed_vig/                         (empty — nothing found; remove this planned folder unless you know where SEED-VIG material actually is)
│   └── raw_downloads/
│       └── doi_10_5061_dryad_..._v20230825 (1).zip
│
└── README.md                             (to be written later)
```

I did **not** rename `xgboost_n2.joblib` to `rest_mode_model.joblib` in this proposal without asking — "n2" is a meaningful, load-bearing name (it's an N2-sleep-stage classifier, a different *kind* of model than a generic "rest mode model"), and your target-structure example implies a generic rename. Tell me whether you want the rename (and if so, I'd do it as a copy, not an in-place rename, until you've confirmed nothing else references the old filename).

---

## 8. Summary — what I need from you before Phase 3 (moves/deletions)

1. Confirm the DROZY dataset duplication finding (§3) — OK to archive the nested folder + zip?
2. Confirm `results/vigilance/` is indeed superseded by `results/dd_continuous_vigilance/` — OK to archive (not delete) it?
3. Rest Mode scaler: confirm intentional absence, or is one missing?
4. Rest Mode hardcoded paths (§5.1): want me to fix these paths (as a separate, explicit task, not part of this cleanup) or leave as-is?
5. Rest Mode `xgboost_n2.joblib` version warning (§5.2): want it re-exported via `Booster.save_model` (separate task), or acceptable as-is for now?
6. `xgboost_n2.joblib` rename to match target structure — yes/no?
7. Whether to search outside `D:\SIH-26` for any relocated SEED-VIG material before writing it off entirely.

**No files have been moved or deleted. Waiting for your review and approval before Phase 3.**
